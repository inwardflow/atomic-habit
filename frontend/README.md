# Frontend

React + TypeScript frontend for Atomic Habits.

## Run locally
```bash
npm ci
npm run dev
```

## Quality checks
```bash
npm run lint
npm run build
```

## Build and preview
```bash
npm run build
npm run preview
```

The app expects backend API access at `http://localhost:8080` unless overridden by runtime configuration.
